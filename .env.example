# ── WhatsApp Cloud API (from Meta Developer Console > WhatsApp > API Setup) ──
WHATSAPP_TOKEN=your_temporary_or_permanent_access_token
WHATSAPP_PHONE_NUMBER_ID=your_test_or_production_phone_number_id
# Found under App Settings > Basic in the Meta Developer Console.
# Used to verify incoming webhooks are genuinely from Meta - strongly
# recommended before exposing this server on a public URL.
WHATSAPP_APP_SECRET=your_app_secret
# Any random string you choose yourself - you'll enter this same value in the
# Meta dashboard when you configure the webhook.
WHATSAPP_VERIFY_TOKEN=choose_any_secret_string_here

# ── Facebook Messenger + Instagram Messaging ──
# These social channels use a separate callback URL: /meta-webhook. Configure
# both under the SAME Meta app so this one META_APP_SECRET can verify webhook
# requests for both channels. META_VERIFY_TOKEN is any random string you choose
# and enter when configuring the Facebook/Instagram webhook subscription.
META_APP_SECRET=your_meta_app_secret
META_VERIFY_TOKEN=choose_another_secret_string_here

# Facebook Page messaging. The token must be a Page access token with the
# permissions required by Meta for Messenger, including pages_messaging.
FACEBOOK_PAGE_ID=
FACEBOOK_PAGE_ACCESS_TOKEN=

# Instagram Messaging through Messenger from Meta > Instagram settings.
# Generate the Page access token there and use the Facebook Page ID shown for
# the Page linked to the Instagram Professional account. This integration uses
# graph.facebook.com/{PAGE_ID}/messages - it does NOT use an Instagram Login
# authorization code or an Instagram User access token.
INSTAGRAM_PAGE_ID=
INSTAGRAM_PAGE_ACCESS_TOKEN=

# ── Meta Marketing API (optional ad-name enrichment) ──
# Separate from the Page messaging tokens above. Use a long-lived/system-user
# token that has read access to the ad account(s) behind the captured Meta ad
# IDs and the ads_read permission. If this is blank, lead attribution still
# works and keeps the exact Meta Ad ID; only Ad / Ad Set / Campaign names stay
# pending until a suitable token is configured later.
META_MARKETING_ACCESS_TOKEN=
# Keep this aligned with the Graph API version used by the Meta app unless a
# deliberate version migration is being tested.
META_MARKETING_API_VERSION=v26.0
# Per-request timeout for read-only ad detail lookups (1s-30s).
META_MARKETING_API_TIMEOUT_MS=10000
# Durable retry worker settings. Failed enrichments stay pending and retry with
# backoff; they never block or delay the patient's chat reply.
META_AD_ENRICHMENT_SWEEP_MS=300000
META_AD_ENRICHMENT_BATCH_SIZE=10

# ── AI reply engine ──
# Preferred first provider. If its configured attempts fail, the reply engine
# can fall through to the other provider when that provider's key is present.
AI_PROVIDER=gemini

# Claude can be either the preferred provider or a fallback after Gemini.
ANTHROPIC_API_KEY=your_anthropic_api_key
# Optional model override.
# CLAUDE_MODEL=claude-sonnet-5

# Gemini. A single key continues to work exactly as before.
GEMINI_API_KEY=your_gemini_api_key
# Optional key pool for quota/rate-limit resilience. You may use either a
# comma/newline-separated list OR numbered variables. Duplicates are removed.
# GEMINI_API_KEYS has no hardcoded key-count limit. The numbered variables below
# are read through _5, in addition to GEMINI_API_KEY itself.
# GEMINI_API_KEYS=key_1,key_2,key_3
# GEMINI_API_KEY_1=your_second_gemini_key
# GEMINI_API_KEY_2=your_third_gemini_key
# GEMINI_API_KEY_3=
# GEMINI_API_KEY_4=
# GEMINI_API_KEY_5=
# The same Gemini pool is used by normal replies, voice transcription, lead
# scoring and follow-up translation. A quota/rate-limit failure immediately
# rotates to another available key. The failed key is skipped during cooldown.
# Short-term rate-limit cooldown default: 60 seconds.
# GEMINI_RATE_LIMIT_COOLDOWN_MS=60000
# Daily-quota-like failures get a longer default cooldown before being tried
# again. This is only used when the provider error clearly indicates a daily/
# quota_exceeded condition; ambiguous 429s use the short rate-limit cooldown.
# GEMINI_QUOTA_COOLDOWN_MS=21600000
# Default temporary provider/network cooldown after retries: 30 seconds.
# GEMINI_UNAVAILABLE_COOLDOWN_MS=30000
# Default cooldown for a rejected/invalid key: 24 hours. A service restart also
# clears in-memory cooldown state, which is appropriate after replacing a key.
# GEMINI_INVALID_KEY_COOLDOWN_MS=86400000
# Customer reply models have safe code defaults:
#   primary:  gemini-3.8-flash
#   fallback: gemini-3.5-flash-lite
# You do NOT need GEMINI_MODEL or GEMINI_FALLBACK_MODEL in Render for normal
# deployments. Keeping model names out of each client's environment avoids
# accidental config drift. The optional overrides below remain available only
# for a deliberate rollback/model test and can normally be left unset.
# GEMINI_MODEL=gemini-3.8-flash
# GEMINI_FALLBACK_MODEL=gemini-3.5-flash-lite
# A model that returns 503/UNAVAILABLE is skipped for subsequent customer
# replies for 60 seconds by default. This is model-level state, separate from
# individual API-key health. Set 0 to disable; maximum accepted value is 10 min.
# GEMINI_MODEL_UNAVAILABLE_COOLDOWN_MS=60000
# Gemini 3 customer replies use low thinking by default for this latency-
# sensitive front-desk workload. Optional override: low | medium | high.
# "minimal" is intentionally not accepted because the primary 3.8 Flash model
# does not support it.
# GEMINI_THINKING_LEVEL=low

# Smart timing for customer-facing Gemini replies. Both Gemini models share the
# same 25-second cap. Normal PR75 key timing remains unchanged: the preferred
# healthy key gets up to 8s; later keys get up to 5s, with adaptive reservation
# for remaining keys. Model fallback uses whatever time remains in that budget.
# GEMINI_REPLY_GLOBAL_BUDGET_MS=25000
# GEMINI_REPLY_PREFERRED_TIMEOUT_MS=8000
# GEMINI_REPLY_FALLBACK_TIMEOUT_MS=5000
# GEMINI_REPLY_MIN_KEY_WINDOW_MS=4000
# Optional: reserve time for the fallback model even when the primary model is
# hanging. Default is 0 so the existing PR75 key timing is preserved.
# GEMINI_REPLY_FALLBACK_MODEL_RESERVE_MS=0
# True 5xx provider failures may retry once after a short 500-1000ms jitter.
# A model-level 503 switches to an available fallback model immediately; if
# there is no fallback model configured, the normal bounded retry still applies.
# GEMINI_REPLY_5XX_RETRY_COUNT=1

# Gemini usageMetadata is recorded automatically for real generation calls:
# customer replies, voice transcription, lead scoring and follow-up translation.
# Setup Status uses the non-generative models.get metadata endpoint instead, so
# running connection checks does not add prompt/output tokens to these totals.
# Only provider/model/purpose/status/token counts/latency are stored; prompts,
# customer messages and API keys are never stored in the usage table.

# These remain fallback-provider controls (for example Claude). Gemini customer
# replies use the global-budget settings above. Other Gemini background tasks
# keep their own existing timing behavior.
AI_REPLY_TIMEOUT_MS=18000
AI_REPLY_RETRY_COUNT=1

# Short quiet window used to combine rapid patient messages such as
# "hi" + "how much hifu" + "for double chin" into one coherent AI reply.
# Values are capped at 3000ms.
AI_REPLY_DEBOUNCE_MS=1200

# Used for date-only business rules such as promotion valid-until dates.
CLINIC_TIMEZONE=Asia/Kuala_Lumpur

# Voice transcription intentionally remains on gemini-2.5-flash in this reply-
# model upgrade. A dedicated transcription-model migration can be tested
# separately. This optional override is only needed for a deliberate test.
# GEMINI_TRANSCRIBE_MODEL=gemini-2.5-flash

# ── Server ──
PORT=3000
# Optional - only needed if your host's proxy setup doesn't let the app
# correctly infer its own public https URL (used to build a link to an
# uploaded promo image for WhatsApp to fetch - see routes/config.js).
# Also used by Telegram alerts to create a direct Inbox link.
# Most hosts (e.g. Render) work fine without setting this for promo images,
# but set it if you want Telegram alerts to include the Inbox link.
# PUBLIC_BASE_URL=https://your-app.onrender.com
# Postgres connection string (e.g. from Neon: Dashboard > Connection Details).
# Use the pooled connection string if offered - safer under concurrent webhook
# traffic. Tables are created automatically on first startup (see src/db/schema.sql).
DATABASE_URL=postgresql://user:password@ep-example-12345.region.aws.neon.tech/neondb?sslmode=require
# Random long string used to sign staff login session cookies. Generate one with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_this_to_a_long_random_string

# ── Telegram alerts (optional) ──
# This one toggle enables both:
# 1) post-conversation lead summaries after the configured inactivity period, and
# 2) immediate Human Intervention Required / channel Delivery Failed alerts.
# Completed score snapshots are queued until the chat is genuinely inactive,
# while human-attention and delivery-failure alerts are sent immediately.
TELEGRAM_ALERTS_ENABLED=false
# Create a bot with @BotFather and paste the token here.
TELEGRAM_BOT_TOKEN=
# Add the bot to the target Telegram group, send a message in the group, then
# use Telegram's getUpdates API or another trusted method to find the group id.
# Group chat ids are commonly negative numbers.
TELEGRAM_CHAT_ID=

# ── Media storage: Cloudflare R2 (from dash.cloudflare.com > R2) ──
# Patient photos/voice notes are uploaded here instead of Postgres.
# Create a bucket, then an R2 API token scoped to Object Read & Write.
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=clinic-chatbot-media
# Keep the bucket PRIVATE - bytes are only ever read back server-side by
# authenticated staff routes (see routes/conversations.js), never exposed
# as a public URL.

# ── First staff login (optional) ──
# If no staff logins exist yet, the server creates one automatically from
# these on startup. Useful on hosts with no shell access (e.g. Render's free
# tier) - set these, deploy, then log in and remove them if you like (safe
# to leave set too; this only ever runs once, when the users table is empty).
ADMIN_USERNAME=
ADMIN_PASSWORD=